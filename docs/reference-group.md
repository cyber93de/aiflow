---
layout: default
title: Reference
nav_order: 6
has_children: true
description: "aiflow reference: the .aiflow/config.json schema, environment variables and tokens, MCP servers wired by aiflow, and the generated project layout."
---

# Reference

Precise, lookup-oriented material:

- **[config.json schema](config-schema)** — every field in `.aiflow/config.json` and what it renders.
- **[Environment variables](environment-variables)** — tokens and keys in `.env` (GitHub/GitLab/…,
  Anthropic API key vs OAuth, context7, router providers).
- **[MCP servers](mcp-servers)** — the MCP servers aiflow wires (github/gitlab/… host MCP, filesystem,
  graphify, cocoindex-code, context7, task-master) and how to add your own.
- **[Project layout](project-layout)** — the files aiflow generates in a project and their roles.
